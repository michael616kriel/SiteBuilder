import { SortProjectPipe } from './sort-project.pipe';

describe('SortProjectPipe', () => {
  it('create an instance', () => {
    const pipe = new SortProjectPipe();
    expect(pipe).toBeTruthy();
  });
});
